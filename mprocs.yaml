scrollback: 10000
procs:
  contracts:
    cwd: packages/contracts
    shell: wait-on -l ../../../dust/packages/world/deploys/31337/latest.json && pnpm run deploy:local
  app:
    cwd: packages/spawn-app
    shell: pnpm run dev --port 3001
  worker:
    cwd: packages/monument-blueprint-worker
    shell: pnpm wrangler dev --port 3002
  tunnel:
    shell: cloudflared tunnel run --token $CLOUDFLARE_TUNNEL_TOKEN
